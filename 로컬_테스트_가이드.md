# 로컬에서 견적문의 폼 테스트하기

로컬 환경에서 견적문의 폼을 테스트하는 방법은 두 가지가 있습니다.

## 방법 1: 로컬 시뮬레이션 모드 (간단한 방법) ✅

가장 간단한 방법입니다. 실제 Netlify로 전송하지 않고, 성공 메시지만 확인할 수 있습니다.

### 사용 방법

1. 로컬 서버 실행:
```bash
python3 -m http.server 8000
```

2. 브라우저에서 접속:
```
http://localhost:8000/contact.html
```

또는

```
http://localhost:8000/contact.html?test=true
```

3. 폼 작성 후 제출:
   - 폼이 정상적으로 제출되는 것처럼 보입니다
   - 성공 모달이 표시됩니다
   - 콘솔에 폼 데이터가 출력됩니다 (개발자 도구 F12 → Console 탭)

### 특징
- ✅ 실제 Netlify로 전송되지 않음
- ✅ 성공 메시지와 UI 동작 확인 가능
- ✅ 콘솔에서 폼 데이터 확인 가능
- ✅ 네트워크 연결 불필요

---

## 방법 2: Netlify Dev CLI (실제 Netlify Forms 테스트) 🚀

실제 Netlify Forms 기능을 로컬에서 테스트할 수 있습니다. 실제로 Netlify에 폼이 제출됩니다.

### 사전 준비

1. Netlify CLI가 이미 설치되어 있습니다 (`package.json` 확인됨)
2. Netlify 계정에 로그인 필요

### 사용 방법

1. Netlify CLI로 로그인:
```bash
npx netlify login
```

2. Netlify Dev 서버 실행:
```bash
npx netlify dev
```

3. 브라우저에서 접속:
```
http://localhost:8888/contact.html
```

4. 폼 작성 후 제출:
   - 실제로 Netlify Forms로 제출됩니다
   - Netlify 대시보드에서 제출 내역을 확인할 수 있습니다
   - 이메일 알림도 정상 작동합니다

### 특징
- ✅ 실제 Netlify Forms로 제출됨
- ✅ Netlify 대시보드에서 제출 내역 확인 가능
- ✅ 이메일 알림 정상 작동
- ⚠️ Netlify 계정 로그인 필요
- ⚠️ 인터넷 연결 필요

---

## 추천 사용 시나리오

### 개발 중 빠른 테스트
→ **방법 1 (로컬 시뮬레이션 모드)** 사용
- UI/UX 확인
- 폼 검증 로직 테스트
- 빠른 반복 테스트

### 배포 전 최종 검증
→ **방법 2 (Netlify Dev CLI)** 사용
- 실제 Netlify Forms 동작 확인
- 이메일 알림 테스트
- 전체 플로우 검증

---

## 주의사항

- **방법 1**은 실제로 데이터가 전송되지 않으므로, 배포 전에 **방법 2**로 최종 테스트하는 것을 권장합니다.
- 배포된 사이트에서는 자동으로 실제 Netlify Forms로 제출됩니다.

