# 홈페이지 개편 계획 분석 보고서

## 📋 개편 목표
- **현재**: 원페이지 스크롤 형식 (고객 이탈율 높음)
- **목표**: 멀티페이지 구조로 전환 (필요 정보만 집중 조회 가능)

---

## ✅ 실행 가능성 종합 분석

### 🎯 전체 평가: **실행 가능 (★★★★★ 5/5)**

모든 단계가 **Vanilla JS + Tailwind CSS + Netlify** 환경에서 실행 가능합니다.

---

## 1단계: 공통 헤더/푸터 분리

### ✅ 실행 가능성: **100% 가능**

#### 현재 상태 분석
- **헤더 위치**: `index.html` 124-145줄
- **푸터 위치**: `index.html` 997-1041줄
- **특징**: 
  - Tailwind CSS 기반
  - Lucide Icons 사용
  - 모바일 햄버거 메뉴 포함
  - Glass morphism 효과 적용

#### 구현 방법 옵션

**옵션 A: Vanilla JS 동적 로드 (추천)**
```javascript
// components.js 생성
async function loadHeader() {
    const response = await fetch('components/header.html');
    const html = await response.text();
    document.getElementById('header-placeholder').innerHTML = html;
    lucide.createIcons(); // 아이콘 재생성
}
```

**장점:**
- ✅ 코드 중복 없음
- ✅ 유지보수 용이
- ✅ Netlify에서 정상 작동

**단점:**
- ⚠️ 각 페이지에서 `lucide.createIcons()` 재실행 필요

**옵션 B: 각 파일에 복사 (간단)**

**장점:**
- ✅ 구현 간단
- ✅ 외부 의존성 없음

**단점:**
- ⚠️ 코드 중복
- ⚠️ 수정 시 모든 파일 업데이트 필요

#### 추천: **옵션 A (Vanilla JS 동적 로드)**
- Netlify는 정적 파일 호스팅이므로 `fetch()`로 HTML 로드 가능
- 코드 재사용성과 유지보수성 우수

---

## 2단계: 메인 페이지 리팩토링

### ✅ 실행 가능성: **100% 가능**

#### 현재 섹션 구조
1. ✅ **Hero Section** (148-195줄) - 유지
2. ✅ **Why NEXO Section** (198-211줄) - **"회사 소개" 여기까지 유지**
3. ❌ **Product Series Section** (214-302줄) - → `product.html`로 이동
4. ❌ **Key Features Section** (305-393줄) - → `product.html`로 이동
5. ❌ **Application Section** (396-478줄) - → `product.html`로 이동
6. ❌ **Video Gallery Section** (479-534줄) - → `video.html`로 이동
7. ⚠️ **Customer Reviews Section** (537-559줄) - → `product.html` 하단에 배치 가능
8. ❌ **Use Cases Section** (686-759줄) - → `product.html`로 이동
9. ❌ **Customers Section** (762-802줄) - → `product.html`로 이동
10. ❌ **A/S System Section** (805-846줄) - → `contact.html` 또는 `product.html`
11. ❌ **FAQ Section** (849-899줄) - → `manual.html` 또는 별도 페이지
12. ✅ **Footer** (997-1041줄) - 공통 컴포넌트로 유지

#### 새로 추가할 링크 카드 섹션

**디자인 요구사항:**
- Grid 레이아웃: `grid-cols-1 md:grid-cols-3 lg:grid-cols-5`
- 컬러: 넥소 네이비 (`#0f172a` 또는 `nexo-navy`)
- Glassmorphism 효과
- Lucide 아이콘 (중앙 상단)
- 호버 효과: `scale-105`, `shadow-2xl`

**카드 구성:**
1. **제품소개** → `product.html`
   - 아이콘: `package` 또는 `monitor`
2. **동영상** → `video.html`
   - 아이콘: `play-circle` 또는 `video`
3. **기술자료** → `resources.html`
   - 아이콘: `file-text` 또는 `download`
4. **사용매뉴얼** → `manual.html`
   - 아이콘: `book-open` 또는 `file-text`
5. **구매문의** → `contact.html`
   - 아이콘: `shopping-cart` 또는 `message-circle`

#### 구현 위치
**Why NEXO Section 바로 아래** (약 212줄 위치)에 카드 섹션 추가

---

## 3단계: 상세 페이지 생성

### ✅ 실행 가능성: **100% 가능**

#### 필요한 HTML 파일 목록

1. **`product.html`**
   - **포함 내용:**
     - Product Series Section (제품 사양 테이블)
     - Key Features Section
     - Application Section
     - Use Cases Section
     - Customers Section
     - Customer Reviews Section (하단)
   - **이미지:** Cloudinary에서 불러오는 그리드 레이아웃
   - **특징:** 기존 코드 재사용 가능

2. **`video.html`**
   - **포함 내용:**
     - Video Gallery Section (현재 479-534줄)
     - YouTube iframe 임베드 (4개 영상)
   - **특징:** 기존 코드 그대로 사용 가능

3. **`resources.html`** (기술자료)
   - **포함 내용:**
     - PDF 다운로드 링크 목록
     - 카테고리별 분류 (사양서, 카탈로그 등)
   - **디자인:** Tailwind CSS 카드 레이아웃
   - **기능:** 파일 다운로드 링크

4. **`manual.html`** (사용매뉴얼)
   - **포함 내용:**
     - 모델명 검색 기능 (JavaScript 필터링)
     - 다운로드 버튼 리스트
     - FAQ 섹션 이동 가능
   - **기능:**
     ```javascript
     // 모델명 검색 예시
     function filterManuals(searchTerm) {
         const manuals = document.querySelectorAll('.manual-card');
         manuals.forEach(card => {
             const modelName = card.dataset.model;
             if (modelName.includes(searchTerm)) {
                 card.style.display = 'block';
             } else {
                 card.style.display = 'none';
             }
         });
     }
     ```

5. **`contact.html`** (구매문의)
   - **포함 내용:**
     - 기존 Contact Modal 내용을 페이지로 전환
     - Netlify Forms 로직 유지
     - A/S System Section 포함 가능
   - **특징:** 기존 폼 코드 재사용

#### 모든 페이지 공통 요구사항
- ✅ Tailwind CSS 설정 동일 (`tailwind.config`)
- ✅ 폰트: Noto Sans KR
- ✅ Lucide Icons 재생성 필요
- ✅ 공통 헤더/푸터 포함

---

## 4단계: 기존 기능 이전 (Gist API & Cloudinary)

### ✅ 실행 가능성: **100% 가능**

#### 현재 기능 위치
- **GitHub Gist API**: `index.html` 1377-1472줄
- **Cloudinary 업로드**: `index.html` 1634-1690줄
- **후기 시스템**: `index.html` 1295-1620줄

#### 모듈화 방법

**옵션 A: `api.js` 파일로 분리 (추천)**
```javascript
// api.js
const CLOUDINARY_CLOUD_NAME = 'dthtfs1mf';
const CLOUDINARY_UPLOAD_PRESET = 'nexo-reviews-unsigned';
const GITHUB_TOKEN = 'YOUR_GITHUB_TOKEN_HERE';
const GIST_ID = 'e7c33232aacc62fdbc03a3437f4e693c';

export async function uploadImageToCloudinary(file) { ... }
export async function getReviews() { ... }
export async function saveReviews(reviews) { ... }
```

**문제점:**
- ⚠️ Vanilla JS는 ES6 모듈 사용 시 `<script type="module">` 필요
- ⚠️ Netlify에서도 작동하지만 모든 HTML에서 모듈 태그 필요

**옵션 B: 전역 함수로 유지 (간단)**
```html
<!-- 각 페이지에서 공통 스크립트 로드 -->
<script src="js/api.js"></script>
```

**장점:**
- ✅ 구현 간단
- ✅ 모든 브라우저 호환
- ✅ Netlify에서 문제없이 작동

#### 추천: **옵션 B (전역 함수)**
- Vanilla JS 환경에 가장 적합
- 유지보수 용이 (중복 코드 최소화)

---

## 5단계: SEO 및 네비게이션 최적화

### ✅ 실행 가능성: **100% 가능**

#### SEO 메타 태그 추가

**각 페이지별 메타 태그 예시:**

```html
<!-- product.html -->
<title>제품소개 - NEXO KOREA 스마트 디스플레이</title>
<meta name="description" content="NEXO NX-Series 제품 사양, 기능, 활용 사례를 확인하세요. 4K UHD 해상도와 멀티 터치 기능을 갖춘 스마트 디스플레이.">

<!-- video.html -->
<title>제품 동영상 - NEXO KOREA</title>
<meta name="description" content="NEXO 스마트 디스플레이 제품 소개 동영상을 시청하세요. 실제 사용 영상과 데모 영상을 제공합니다.">

<!-- manual.html -->
<title>사용매뉴얼 - NEXO KOREA 다운로드</title>
<meta name="description" content="NEXO 스마트 디스플레이 사용매뉴얼을 다운로드하세요. 모델별 매뉴얼과 FAQ를 제공합니다.">

<!-- resources.html -->
<title>기술자료 - NEXO KOREA</title>
<meta name="description" content="NEXO 스마트 디스플레이 기술 사양서, 카탈로그, 인증서를 다운로드하세요.">

<!-- contact.html -->
<title>구매문의 - NEXO KOREA 견적 문의</title>
<meta name="description" content="NEXO 스마트 디스플레이 구매 문의 및 견적 요청을 보내주세요. 전문 상담사가 빠르게 연락드립니다.">
```

#### 네비게이션 링크 수정

**헤더 네비게이션 업데이트:**
```html
<a href="index.html">홈</a>
<a href="product.html">제품소개</a>
<a href="video.html">동영상</a>
<a href="resources.html">기술자료</a>
<a href="manual.html">사용매뉴얼</a>
<a href="contact.html">구매문의</a>
```

#### Netlify Redirects 설정

**`_redirects` 파일 생성:**
```
# 기존 해시 링크 리다이렉트
/products /product.html 200
/features /product.html#features 200
/applications /product.html#applications 200
/solution /product.html#solution 200
/customers /product.html#customers 200
/reviews /product.html#reviews 200
/faq /manual.html#faq 200
/contact /contact.html 200
```

---

## 🔍 잠재적 이슈 및 해결 방안

### 이슈 1: Netlify Forms 경로 변경
**문제:** `contact.html`로 이동 시 Netlify Forms가 작동하지 않을 수 있음

**해결:**
- ✅ Netlify Forms는 모든 HTML 파일에서 작동
- ✅ `form` 태그에 `netlify` 속성만 있으면 됨
- ✅ `name` 속성은 고유하게 유지

### 이슈 2: Lucide Icons 초기화
**문제:** 동적으로 로드된 페이지에서 아이콘이 표시되지 않을 수 있음

**해결:**
- ✅ 각 페이지 로드 시 `lucide.createIcons()` 실행
- ✅ 동적 콘텐츠 추가 후에도 재실행 필요

### 이슈 3: 모바일 메뉴 링크
**문제:** 모바일 햄버거 메뉴 링크도 업데이트 필요

**해결:**
- ✅ 헤더 컴포넌트에 모바일 메뉴 포함
- ✅ 모든 페이지에서 동일한 네비게이션 사용

### 이슈 4: 기존 URL 접근성
**문제:** 기존에 공유된 URL이 깨질 수 있음

**해결:**
- ✅ `_redirects` 파일로 리다이렉트 설정
- ✅ 검색 엔진 인덱싱 유지

---

## 📊 작업 우선순위 및 예상 소요 시간

### Phase 1: 기본 구조 (2-3시간)
1. ✅ 공통 헤더/푸터 분리 (옵션 B: 복사 방식)
2. ✅ 메인 페이지 리팩토링 (Why NEXO까지만 + 카드 섹션)

### Phase 2: 상세 페이지 생성 (4-5시간)
3. ✅ `product.html` 생성
4. ✅ `video.html` 생성
5. ✅ `contact.html` 생성

### Phase 3: 추가 페이지 및 기능 (3-4시간)
6. ✅ `resources.html` 생성
7. ✅ `manual.html` 생성 (검색 기능 포함)
8. ✅ 기존 기능 모듈화 (`api.js`)

### Phase 4: 최적화 (1-2시간)
9. ✅ SEO 메타 태그 추가
10. ✅ 네비게이션 링크 수정
11. ✅ `_redirects` 파일 설정

**총 예상 시간: 10-14시간**

---

## 🎯 최종 권장사항

### ✅ 실행 권장

**이유:**
1. ✅ 모든 기술 스택이 현재 환경에서 지원됨
2. ✅ Vanilla JS + Tailwind CSS로 충분히 구현 가능
3. ✅ Netlify 정적 호스팅에 적합한 구조
4. ✅ 기존 코드 재사용 가능 (리팩토링 효율성 높음)
5. ✅ SEO 및 사용자 경험 개선 효과 기대

### 🚀 구현 시작 단계

**1단계부터 순차적으로 진행하되, 다음을 권장:**
1. 공통 헤더/푸터는 **복사 방식 (옵션 B)**으로 시작
   - 빠른 구현 가능
   - 나중에 동적 로드로 전환 가능
2. 메인 페이지 리팩토링부터 시작
   - 즉시 효과 확인 가능
   - 사용자 테스트 용이
3. 상세 페이지는 **product.html**부터 생성
   - 가장 많은 콘텐츠 포함
   - 다른 페이지 템플릿으로 활용 가능

---

## 📝 추가 고려사항

### 성능 최적화
- 이미지 lazy loading 유지
- Cloudinary CDN 활용
- 불필요한 JavaScript 최소화

### 접근성
- 키보드 네비게이션 지원
- ARIA 라벨 추가
- 색상 대비 검증

### 반응형 디자인
- 모바일 카드 레이아웃 (1열)
- 태블릿 레이아웃 (3열)
- 데스크톱 레이아웃 (5열)

---

**작성일:** 2024년
**분석자:** AI Assistant
**상태:** ✅ 실행 가능 - 구현 권장

